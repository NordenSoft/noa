# MOVED → `noa-trust-plan.md`

This file is a signpost, not a plan. The single canonical plan is **`noa-trust-plan.md`** (owner
decision 2026-07-31 — one file, an easy name to remember).

Renamed with `git mv`, so `git log --follow noa-trust-plan.md` carries the full history.

**Do not write blockers, status or plan content here.** If you find content below this line, an agent
wrote to a stale path — move it into `noa-trust-plan.md` and restore this signpost. Two plan files is
the exact duplication the owner forbade.
